# Monorepo root — each subproject (web/, ios/) has its own .gitignore too.

# macOS
.DS_Store

# Claude Code local state
.claude/

# env files
.env*

# iOS / Xcode
ios/build/
ios/DerivedData/
ios/**/xcuserdata/
ios/**/*.xcuserstate
ios/.swiftpm/
